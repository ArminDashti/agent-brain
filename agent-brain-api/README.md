# agent-brain-api

Gin + PostgreSQL + Qdrant knowledge API.

```bash
cp .env.example .env
docker compose up -d
go mod tidy
go run ./cmd/server
```

Login: `armin` / `dopadopa123` — ports API `:8220`.
